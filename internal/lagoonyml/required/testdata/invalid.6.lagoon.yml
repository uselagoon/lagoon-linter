environments:
  main:
    cronjobs:
      - name: multiline cron command
        schedule: "15 1 * * *"
        command: |-
            echo "multiline \
            cron command"
        service: cli
